/**
 * Absolute origin for metadata. Open Graph and sitemap URLs must be absolute,
 * so relative paths silently produce broken preview cards without this.
 */
export const SITE_URL = "https://shlokthakkar.com";
